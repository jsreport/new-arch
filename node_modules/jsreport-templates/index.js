const main = require('./lib/templates.js')
const config = require('./jsreport.config.js')
const path = require('path')

module.exports = function (options) {
  config.options = options
  config.directory = __dirname
  config.main = main 
  return config
}
