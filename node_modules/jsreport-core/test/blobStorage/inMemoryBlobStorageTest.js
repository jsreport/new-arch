require('should')
const InMemoryBlobStorage = require('../../lib/main/blobStorage/inMemoryBlobStorageProvider')
const common = require('./common.js')

describe('inMemoryBlobStorage', () => {
  let storage

  beforeEach(() => (storage = InMemoryBlobStorage({})))
  common(() => storage)
})
